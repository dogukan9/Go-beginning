package main

func main() {

	// var card string = "Ace of spades"
	// card := "Ace of spades"
	// card := newCard()
	// fmt.Println(card)

	//-------------------------------------------------
	// cardArray := []string{newCard(), newCard()}

	// cardArray = append(cardArray, "What") //add
	// // fmt.Println(cardArray)
	// for i, cardItem := range cardArray {
	// 	fmt.Println(i, cardItem)
	// }

	//-------------------------------------------------
	// cardArray := deck{newCard(), newCard()}

	// cardArray = append(cardArray, "What") //add
	// // fmt.Println(cardArray)
	// for i, cardItem := range cardArray {
	// 	fmt.Println(i, cardItem)
	// }
	//-------------------------------------------------
	// cardArray := deck{newCard(), newCard()}
	// cardArray = append(cardArray, "What") //add
	// cardArray.print()
	//-------------------------------------------------

	// cards := newDeck()
	// cards.print()

	//-------------------------------------------------

	// cards := newDeck()

	// fmt.Println(len(cards))

	// hand, remainingCards := deal(cards, 9)

	// hand.print()
	// fmt.Println("----------------------------")
	// remainingCards.print()

	//-------------------------------------------------

	// greeting := "Hi there!"

	// fmt.Println([]byte(greeting))

	//-------------------------------------------------
	// cards := newDeck()
	// fmt.Println(cards.toString())
	// //-------------------------------------------------
	// cards := newDeck()
	// cards.saveToFile("my_cards")
	//-------------------------------------------------

	// cards := newDeckFromFile("my_cards")
	// cards.print()
	//-------------------------------------------------

	// 	cards := newDeck()
	// 	cards.shuffle()
	// 	cards.print()
}

// func newCard() string {

// 	return "Five of Dimands"
// }
